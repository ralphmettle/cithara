from django.db import models

# Create your models here.
# class NoteModel(models.Model):
#     note_string = models.CharField(max_length=5)
#     created_at = models.DateTimeField(auto_now_add=True)

#     def __str__(self):
#         return self.name
